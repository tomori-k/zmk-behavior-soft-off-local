# Delete all unused properties.

# Give your module a name
name: zmk-behavior-softoff-local
build:
  # Folder containing a "CMakeLists.txt" (. = root)
  cmake: .
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    # Folder containing a "boards" folder (. = root)
    board_root: .
    # Folder containing a "dts" folder (. = root)
    dts_root: .
